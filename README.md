# Calculadora-ney

## Setup

1. Install dependencies:

   ```bash
   npm install
   ```

2. Copy `.env.example` to `.env` and fill in the values.

3. Run the type checker:

   ```bash
   npm run check
   ```
